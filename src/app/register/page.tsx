import RegisterCard from "@/src/components/registe-card";

export default function Register() {
    return (
        <div className="flex items-center justify-center h-screen">
            <RegisterCard />
        </div>
    );
}